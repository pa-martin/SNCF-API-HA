{
  "name": "sncf-train-card-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/copy-to-www.mjs",
    "smoke": "npm run build && node scripts/smoke.mjs",
    "watch": "vite build --watch",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "esbuild": "^0.28.0",
    "linkedom": "^0.18.12",
    "typescript": "^6.0.3",
    "vite": "^8.0.13"
  }
}
